from setuptools import setup
setup(
        name='snowflake',
        version='1.0',
        install_requires=[''],
        entry_points={"console_scripts": ["snowflake=snowflake.main:main"]},
        url="https://github.com/winter1245/snowflake",
      )
